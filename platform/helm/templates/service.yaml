{{- range .Values.services }}
{{- if .enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
spec:
  selector: { app: {{ .name }} }
  ports:
  - name: http
    port: 80
    targetPort: {{ .containerPort | default 8080 }}
{{- end }}
{{- end }}