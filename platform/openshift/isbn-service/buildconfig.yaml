apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: isbn-service
  namespace: antonio-dimarino-dev
spec:
  output:
    to:
      kind: ImageStreamTag
      name: isbn-service:latest
  source:
    type: Binary
  strategy:
    type: Docker
  successfulBuildsHistoryLimit: 2
  failedBuildsHistoryLimit: 2