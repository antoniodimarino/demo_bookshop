apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: <nome_progetto_openshift>
spec:
  selector: { app: payment-service }
  ports:
  - name: http
    port: 80
    targetPort: 8080