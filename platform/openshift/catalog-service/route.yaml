apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: catalog-service
  namespace: <nome_progetto_openshift>
spec:
  to: { kind: Service, name: catalog-service }
  port:
    targetPort: http